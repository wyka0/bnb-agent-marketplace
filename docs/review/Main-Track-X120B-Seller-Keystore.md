# X.120B Testnet Seller Keystore

**Mode:** Local keystore initialization gate only. No blockchain transaction, seller registration, endpoint exposure, ERC-8183 publish, buyer funding, marketplace production change, commit, push, or deployment was performed.

## Keystore Result

**KEYSTORE CREATED: BLOCKED**

Checks:

- `C:\Users\rashe\.bnbagent\wallets` exists: **NO**
- EVM Keystore V3 file exists: **NO**
- `WALLET_PASSWORD`: **MISSING**
- `PRIVATE_KEY`: **MISSING**
- `NETWORK`: **PRESENT**, expected `bsc-testnet`
- `ERC8183_SERVICE_PRICE`: **PRESENT**, `1000000000000000000`
- `.env.local`: created at repository root with empty credential fields only; it is gitignored

No secret values were printed, stored, or generated by this operation.

## Seller Scaffold Change

Only the isolated `services/v2-seller/seller.ts` was updated:

- Imports and calls `loadEnv()` before reading `process.env`.
- Requires `NETWORK=bsc-testnet`.
- Requires `WALLET_PASSWORD`.
- Requires `PRIVATE_KEY` only when no Keystore V3 exists.
- Constructs `EVMWalletProvider` with `privateKey: process.env.PRIVATE_KEY || undefined`.
- Reports only the public address after an imported wallet is encrypted.

No marketplace source or production activation code was modified.

## Seller Public Address

No new seller EVM Keystore was available, so no new seller address could be derived.

The existing onchainos managed-wallet address was not converted, used, or exposed as an EVM seller key:

- Existing managed address: intentionally not reused
- Buyer separation: not applicable because no seller EVM keystore was initialized

## Chain / Funding

- Target chain: BSC Testnet, chain `97`
- Seller tBNB balance: not checked for a new seller because no seller keystore/address exists
- Required target after provisioning: approximately `0.05 tBNB` and approximately `5 $U`, subject to current SDK/faucet requirements
- No mainnet funds or transaction used

## Verification

- Repository typecheck: **PASS**
- Repository lint: **PASS**
- Seller file lint/type context: **PASS**
- Keystore created: **BLOCKED**
- Seller address: **NOT AVAILABLE**
- Chain verification: **NOT RUN** without seller wallet
- Balance verification: **NOT RUN** without seller wallet
- Private key removed: **NO / NOT APPLICABLE** because no private key was supplied

## Required Local Action

Provision a dedicated disposable BSC Testnet EVM Keystore V3 and local `WALLET_PASSWORD`. The first-run `PRIVATE_KEY` must remain local only, be used once for SDK encryption, and then be removed manually from `.env.local` after successful initialization.

Do not paste the private key, mnemonic, seed phrase, or password into chat. Do not convert the existing managed onchainos wallet.

After provisioning, rerun X.120B. Do not proceed to ERC-8004 registration, `/negotiate`, ERC-8183 publish, or buyer funding until the seller-ready checks pass.
