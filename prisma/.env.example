# Shared database environment for local development.
# Used by prisma/, docker-compose, and the API/worker later.
# Copy to .env and set real values for your project.

DATABASE_URL="postgresql://postgres:postgres@localhost:5432/marketplace"